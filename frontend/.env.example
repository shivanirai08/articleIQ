# Copy to `.env.local` (gitignored). Safe to commit this template only.

# FastAPI backend URL — must match backend CORS_ORIGINS (http://localhost:3000)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
