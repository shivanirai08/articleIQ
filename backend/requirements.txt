# ArticleIQ backend dependencies (Checkpoint 3+)
#
# fastapi  - HTTP API framework (routes, validation, OpenAPI docs)
# uvicorn  - ASGI server that runs the FastAPI app
#
# Pinned after successful local install for academic reproducibility.
# Later checkpoints will add: pydantic-settings, spacy, Gemini SDK, pytest, httpx, ...

fastapi==0.139.0
uvicorn[standard]==0.51.0
